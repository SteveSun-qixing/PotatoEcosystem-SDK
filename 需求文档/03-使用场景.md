# 使用场景

## 1. 场景概述

本文档详细描述 Chips SDK 在不同应用类型和业务场景中的具体使用方式,帮助开发者快速理解如何将 SDK 集成到自己的项目中。

## 2. 笔记类应用

### 2.1 场景描述

用户使用笔记应用记录各种信息,包括文字、图片、代码、清单等。应用需要提供强大的编辑功能和良好的查看体验。

### 2.2 需求分析

**核心需求**：
- 创建和编辑多种类型的笔记
- 快速搜索笔记内容
- 组织和分类笔记
- 同步到云端

**用户期望**：
- 编辑流畅,响应快速
- 支持富文本格式
- 可以插入图片和附件
- 离线可用

### 2.3 SDK 集成方案

#### 2.3.1 初始化

```javascript
import ChipsSDK from '@chips/sdk';

// 初始化 SDK
const chips = new ChipsSDK({
  storage: 'indexeddb',  // 使用 IndexedDB 存储
  cache: true,
  offline: true
});
```

#### 2.3.2 创建笔记

```javascript
// 创建编辑器
const editor = chips.createEditor('#note-editor', {
  mode: 'wysiwyg',
  toolbar: [
    'bold', 'italic', 'underline',
    'heading', 'list', 'link',
    'image', 'code', 'quote'
  ],
  autoSave: true,
  placeholder: '开始记录...'
});

// 监听内容变化
editor.on('change', async () => {
  const card = await editor.getCard();
  await saveNote(card);
});
```

#### 2.3.3 展示笔记列表

```javascript
// 加载笔记列表
const notes = await chips.loadCards(noteFiles);

// 渲染笔记预览
notes.forEach(note => {
  const preview = chips.renderPreview(note, {
    maxLength: 200,
    showThumbnail: true
  });
  
  notesContainer.appendChild(preview);
});
```

#### 2.3.4 搜索功能

```javascript
// 搜索笔记
const searchResults = await chips.search(query, {
  fields: ['content', 'title', 'tags'],
  highlight: true
});

// 显示搜索结果
displaySearchResults(searchResults);
```

### 2.4 完整示例

```javascript
class NoteApp {
  constructor() {
    this.chips = new ChipsSDK({ offline: true });
    this.editor = null;
    this.currentNote = null;
  }
  
  async createNote() {
    // 创建新笔记
    this.currentNote = await this.chips.createCard({
      type: 'composite',
      metadata: {
        title: 'New Note',
        createdAt: Date.now(),
        tags: []
      }
    });
    
    // 加载到编辑器
    this.editor.loadCard(this.currentNote);
  }
  
  async saveNote() {
    // 获取编辑内容
    const updated = await this.editor.getCard();
    
    // 保存到本地
    await this.chips.saveCard(updated, `notes/${updated.id}.card`);
    
    // 同步到云端
    await this.syncToCloud(updated);
  }
  
  async loadNote(id) {
    // 从本地或云端加载
    this.currentNote = await this.chips.loadCard(`notes/${id}.card`);
    this.editor.loadCard(this.currentNote);
  }
  
  async searchNotes(query) {
    return await this.chips.search(query, {
      path: 'notes/',
      highlight: true
    });
  }
}
```

## 3. 博客系统

### 3.1 场景描述

博客系统需要展示文章内容,支持评论、分享等社交功能。文章使用卡片格式存储和展示。

### 3.2 需求分析

**核心需求**：
- 发布和展示文章
- SEO 友好
- 响应式设计
- 评论系统

**用户期望**：
- 阅读体验好
- 加载速度快
- 移动端友好
- 可以分享

### 3.3 SDK 集成方案

#### 3.3.1 文章展示

```javascript
// 服务端渲染（SSR）
import { ChipsSDK } from '@chips/sdk/node';

// 渲染文章为 HTML
export async function renderArticle(articleId) {
  const chips = new ChipsSDK();
  const article = await chips.loadCard(`articles/${articleId}.card`);
  
  // 渲染为 HTML 字符串
  const html = await chips.renderToHTML(article, {
    theme: 'blog-theme',
    seo: true,
    meta: {
      title: article.metadata.title,
      description: article.metadata.description,
      image: article.metadata.coverImage
    }
  });
  
  return html;
}
```

#### 3.3.2 客户端增强

```javascript
// 客户端脚本
import ChipsSDK from '@chips/sdk';

const chips = new ChipsSDK();

// 增强已渲染的文章
chips.enhance('#article-content', {
  // 启用交互功能
  interactive: true,
  
  // 懒加载图片
  lazyLoadImages: true,
  
  // 代码高亮
  highlightCode: true,
  
  // 目录导航
  toc: true
});

// 添加评论功能
chips.attachComments('#article-content', {
  articleId: currentArticleId,
  api: '/api/comments'
});
```

#### 3.3.3 文章编辑

```javascript
// 作者编辑文章
const editor = chips.createEditor('#editor', {
  mode: 'markdown',
  preview: true,
  
  // 自定义工具栏
  toolbar: [
    'heading', 'bold', 'italic',
    'link', 'image', 'code',
    'preview', 'publish'
  ],
  
  // 图片上传
  uploadImage: async (file) => {
    const url = await uploadToServer(file);
    return url;
  }
});

// 发布文章
editor.on('publish', async () => {
  const article = await editor.getCard();
  
  // 保存到服务器
  await fetch('/api/articles', {
    method: 'POST',
    body: JSON.stringify(article)
  });
  
  // 跳转到文章页面
  window.location.href = `/article/${article.id}`;
});
```

### 3.4 SEO 优化

```javascript
// 生成 Open Graph meta 标签
const metaTags = chips.generateMetaTags(article, {
  type: 'article',
  site: 'My Blog',
  author: article.metadata.author
});

// 生成 JSON-LD
const jsonLD = chips.generateJSONLD(article, {
  '@type': 'BlogPosting',
  publisher: {
    '@type': 'Organization',
    name: 'My Blog'
  }
});
```

## 4. 在线教育平台

### 4.1 场景描述

在线教育平台使用卡片组织课程内容,包括视频讲解、图文教程、练习题等。

### 4.2 需求分析

**核心需求**：
- 组织课程结构
- 播放视频和音频
- 交互式练习
- 学习进度跟踪

**用户期望**：
- 学习体验流畅
- 内容易于理解
- 可以做笔记
- 移动学习支持

### 4.3 SDK 集成方案

#### 4.3.1 课程结构

```javascript
// 创建课程
const course = await chips.createBox({
  name: 'JavaScript 入门教程',
  layout: 'list',
  metadata: {
    author: 'John Doe',
    level: 'beginner',
    duration: '10 hours'
  }
});

// 添加课程章节
await course.addCard({
  type: 'composite',
  metadata: { title: '第1章: 变量和类型' },
  children: [
    { type: 'video', url: 'lesson1.mp4' },
    { type: 'markdown', content: '## 知识点\n...' },
    { type: 'quiz', questions: [...] }
  ]
});
```

#### 4.3.2 视频课程

```javascript
// 渲染视频课程
const videoLesson = await chips.renderCard(lesson, '#player', {
  controls: {
    playbackRate: true,
    subtitles: true,
    notes: true
  },
  
  // 学习进度跟踪
  onProgress: (progress) => {
    saveProgress(courseId, lessonId, progress);
  },
  
  // 做笔记
  onNote: (time, note) => {
    saveNote(courseId, lessonId, time, note);
  }
});
```

#### 4.3.3 交互式练习

```javascript
// 创建练习题卡片
const quiz = await chips.createCard({
  type: 'quiz',
  content: {
    questions: [
      {
        type: 'multiple-choice',
        question: 'JavaScript 中哪个关键字用于声明变量?',
        options: ['var', 'let', 'const', '以上都是'],
        correct: 3
      },
      {
        type: 'code',
        question: '补全以下代码',
        template: 'function add(a, b) {\n  return ___;\n}',
        answer: 'a + b'
      }
    ]
  }
});

// 渲染练习
const quizRenderer = chips.renderCard(quiz, '#quiz', {
  interactive: true,
  showAnswers: false,
  
  onSubmit: (answers) => {
    const score = evaluateAnswers(answers);
    showResult(score);
  }
});
```

#### 4.3.4 学习进度

```javascript
// 跟踪学习进度
class LearningProgress {
  constructor(chips, userId, courseId) {
    this.chips = chips;
    this.userId = userId;
    this.courseId = courseId;
  }
  
  async markComplete(lessonId) {
    await this.saveProgress(lessonId, {
      status: 'completed',
      completedAt: Date.now()
    });
    
    // 检查是否完成整个课程
    if (await this.isCourseComplete()) {
      await this.awardCertificate();
    }
  }
  
  async getProgress() {
    const course = await this.chips.loadBox(this.courseId);
    const lessons = course.getCards();
    const completed = lessons.filter(l => this.isComplete(l.id));
    
    return {
      total: lessons.length,
      completed: completed.length,
      percentage: (completed.length / lessons.length) * 100
    };
  }
}
```

## 5. 知识库系统

### 5.1 场景描述

企业知识库用于存储和管理公司的文档、规范、教程等知识资产。

### 5.2 需求分析

**核心需求**：
- 文档组织和分类
- 全文搜索
- 版本控制
- 权限管理
- 协作编辑

**用户期望**：
- 快速找到需要的信息
- 文档结构清晰
- 易于编辑和更新
- 支持团队协作

### 5.3 SDK 集成方案

#### 5.3.1 文档结构

```javascript
// 创建知识库
const wiki = await chips.createBox({
  name: '公司知识库',
  layout: 'wiki',
  metadata: {
    organization: 'My Company'
  }
});

// 组织文档树
const structure = {
  '产品': {
    '产品规格': 'docs/product/specs.card',
    '用户手册': 'docs/product/manual.card'
  },
  '开发': {
    'API文档': 'docs/dev/api.card',
    '代码规范': 'docs/dev/standards.card'
  },
  '运营': {
    '操作流程': 'docs/ops/procedures.card'
  }
};
```

#### 5.3.2 文档展示

```javascript
// 渲染文档页面
async function renderDocPage(docPath) {
  const doc = await chips.loadCard(docPath);
  
  await chips.renderCard(doc, '#doc-content', {
    mode: 'wiki',
    
    // 显示目录
    toc: {
      enabled: true,
      position: 'left',
      sticky: true
    },
    
    // 面包屑导航
    breadcrumb: true,
    
    // 编辑按钮（如果有权限）
    editable: hasEditPermission(),
    
    // 版本信息
    showVersion: true,
    showLastModified: true
  });
}
```

#### 5.3.3 搜索功能

```javascript
// 全文搜索
async function searchDocs(query) {
  const results = await chips.search(query, {
    path: 'docs/',
    
    // 搜索选项
    fuzzy: true,
    highlight: true,
    
    // 排序
    sortBy: 'relevance',
    
    // 过滤
    filter: {
      type: ['markdown', 'richtext'],
      tags: selectedTags
    }
  });
  
  // 显示结果
  displaySearchResults(results);
}
```

#### 5.3.4 版本管理

```javascript
// 集成 Git 版本控制
const docManager = chips.createDocumentManager({
  versionControl: {
    type: 'git',
    repo: '/path/to/repo'
  }
});

// 保存文档时自动提交
await docManager.save(doc, {
  commit: true,
  message: '更新 API 文档',
  author: currentUser
});

// 查看历史版本
const history = await docManager.getHistory(docPath);

// 回滚到指定版本
await docManager.rollback(docPath, versionId);
```

#### 5.3.5 协作编辑

```javascript
// 启用协作编辑
const editor = chips.createCollaborativeEditor('#editor', {
  document: docPath,
  
  // WebSocket 连接
  connection: {
    url: 'wss://example.com/collab',
    token: authToken
  },
  
  // 显示其他用户光标
  showCursors: true,
  
  // 显示在线用户
  showUsers: true
});

// 监听协作事件
editor.on('user-join', (user) => {
  console.log(`${user.name} 加入了编辑`);
});

editor.on('user-leave', (user) => {
  console.log(`${user.name} 离开了编辑`);
});
```

## 6. 移动应用

### 6.1 场景描述

移动 App 需要查看和编辑卡片内容,提供良好的触控体验。

### 6.2 需求分析

**核心需求**：
- 触控优化的交互
- 离线支持
- 快速启动
- 省电省流量

**用户期望**：
- 操作流畅
- 响应快速
- 占用空间小
- 可以离线使用

### 6.3 SDK 集成方案（iOS）

```swift
import ChipsSDK

class NoteViewController: UIViewController {
    var chips: ChipsSDK!
    var renderer: CardRenderer!
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        // 初始化 SDK
        chips = ChipsSDK(config: ChipsConfig(
            storage: .fileSystem,
            cache: true,
            offline: true
        ))
        
        // 创建渲染器
        renderer = chips.createRenderer(in: containerView)
    }
    
    func loadCard(path: String) async {
        do {
            let card = try await chips.loadCard(path: path)
            try await renderer.render(card: card, options: RenderOptions(
                theme: .system,
                interactive: true
            ))
        } catch {
            showError(error)
        }
    }
    
    func createNote() async {
        let editor = chips.createEditor(in: containerView)
        
        // 配置编辑器
        editor.setToolbar([
            .bold, .italic, .heading,
            .image, .camera, .location
        ])
        
        // 监听保存
        editor.onSave { [weak self] card in
            await self?.saveCard(card)
        }
    }
}
```

### 6.4 SDK 集成方案（Android）

```kotlin
import com.chips.sdk.ChipsSDK
import com.chips.sdk.CardRenderer

class NoteActivity : AppCompatActivity() {
    private lateinit var chips: ChipsSDK
    private lateinit var renderer: CardRenderer
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_note)
        
        // 初始化 SDK
        chips = ChipsSDK.Builder()
            .setStorage(StorageType.FILE_SYSTEM)
            .enableCache(true)
            .enableOffline(true)
            .build()
        
        // 创建渲染器
        renderer = chips.createRenderer(containerView)
    }
    
    suspend fun loadCard(path: String) {
        try {
            val card = chips.loadCard(path)
            renderer.render(card, RenderOptions(
                theme = Theme.SYSTEM,
                interactive = true
            ))
        } catch (e: Exception) {
            showError(e)
        }
    }
    
    fun createNote() {
        val editor = chips.createEditor(containerView)
        
        // 配置工具栏
        editor.setToolbar(listOf(
            Toolbar.BOLD,
            Toolbar.ITALIC,
            Toolbar.IMAGE,
            Toolbar.CAMERA
        ))
        
        // 监听保存
        editor.setOnSaveListener { card ->
            lifecycleScope.launch {
                saveCard(card)
            }
        }
    }
}
```

## 7. 内容平台

### 7.1 场景描述

内容平台（如社区、论坛、问答网站）使用卡片作为内容发布格式。

### 7.2 SDK 集成方案

#### 7.2.1 内容发布

```javascript
// 发布内容编辑器
const publishEditor = chips.createEditor('#publish-editor', {
  mode: 'wysiwyg',
  
  // 丰富的工具栏
  toolbar: [
    'heading', 'bold', 'italic',
    'link', 'image', 'video',
    'code', 'quote', 'list',
    'emoji', 'mention', 'hashtag'
  ],
  
  // @提及用户
  mentions: {
    source: async (query) => {
      return await searchUsers(query);
    },
    renderItem: (user) => {
      return `@${user.username}`;
    }
  },
  
  // #话题标签
  hashtags: {
    source: async (query) => {
      return await searchHashtags(query);
    }
  }
});
```

#### 7.2.2 内容展示

```javascript
// 渲染内容流
async function renderFeed(posts) {
  const feed = document.getElementById('feed');
  
  for (const post of posts) {
    const card = await chips.loadCard(post.cardPath);
    
    // 创建帖子容器
    const postElement = document.createElement('div');
    postElement.className = 'post';
    
    // 渲染卡片内容
    const cardElement = await chips.renderCard(card, postElement, {
      compact: true,
      maxHeight: 500,
      expandable: true
    });
    
    // 添加互动按钮
    const actions = createPostActions(post);
    postElement.appendChild(actions);
    
    feed.appendChild(postElement);
  }
}

function createPostActions(post) {
  const actions = document.createElement('div');
  actions.innerHTML = `
    <button onclick="like(${post.id})">👍 ${post.likes}</button>
    <button onclick="comment(${post.id})">💬 ${post.comments}</button>
    <button onclick="share(${post.id})">🔗 分享</button>
  `;
  return actions;
}
```

## 8. 数据可视化

### 8.1 场景描述

使用卡片展示数据报表、图表和仪表板。

### 8.2 SDK 集成方案

```javascript
// 创建数据可视化卡片
const dashboard = await chips.createCard({
  type: 'composite',
  layout: 'grid',
  children: [
    {
      type: 'chart',
      chartType: 'line',
      data: salesData,
      title: '销售趋势'
    },
    {
      type: 'chart',
      chartType: 'pie',
      data: categoryData,
      title: '分类占比'
    },
    {
      type: 'metric',
      value: totalRevenue,
      label: '总收入',
      trend: '+15%'
    }
  ]
});

// 实时更新数据
dashboard.on('data-update', async (newData) => {
  await dashboard.updateData(newData);
});
```

## 9. 总结

Chips SDK 在各种场景下都能提供强大而灵活的支持：

**关键优势**：
- ⚡ **快速集成**：几行代码即可开始使用
- 🎨 **灵活定制**：适应不同应用的需求
- 📱 **跨平台**：Web、桌面、移动端统一 API
- 🔧 **功能丰富**：满足复杂业务需求
- 🚀 **性能优秀**：优化的渲染和加载策略

无论是笔记应用、博客系统、在线教育还是企业知识库，Chips SDK 都能提供最佳的集成方案。
